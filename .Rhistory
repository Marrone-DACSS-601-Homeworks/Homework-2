2*2
"Hello, World!"
<-"Hello, World!"
greeting <- "Hello, World!"
my_vector <- c(1,2,3,4)
install.packages("package_name")
installed.packages(tidyverse)
install.packages("tidyverse")
install.packages("library(tidyverse)")
(.packages())
help("read.csv")
?readr
help(help)
help('CO2')
world_population <- read.csv("~/Downloads/world_population.csv", header=FALSE)
View(world_population)
world_population <- read.csv("~/Downloads/world_population.csv")
View(world_population)
data()
head()
head(CO2)
tail(CO2)
colnames(co2)
colnames(CO2)
dim(CO2)
str(CO2)
summary(CO2)
summary(CO2$uptake)
mediam(CO2$uptake)
median(CO2$uptake)
class(CO2$uptake)
unique(CO2$Treatment)
subset(CO2, conc == min(CO2$conc))
hist(CO2$uptake)
plot(Orange$age, Orange$circumference)
plot(Orange$age, Orange$circumference, )
plot(Orange$age, Orange$circumference
plot(Orange$age, Orange$circumference,
plot(Orange$age, Orange$circumference, xlab="Age", ylab="Circumference", main="Circumference vs. Age", col="blue", pch=16)
oceans <- c("Arctic", "Atlantic", "Indian", "Pacific", "Southern")
avg_depth <- c(1.2, 3.65, 3.74, 3.97, 3.27)
oceans_depth <- data.frame(oceans, avg_depth)
print(oceans_depth)
linkGit="https://github.com/DACSS-Fundamentals/overview/raw/refs/heads/main/FSI-2023-DOWNLOAD.xlsx" ## code to request a file from a specific link
fragility23=rio::import(file = linkGit) ## code to use the package rio, import the file above and name it 'fragility23'
linkGit="https://github.com/DACSS-Fundamentals/overview/raw/refs/heads/main/FSI-2023-DOWNLOAD.xlsx" ## code to request a file from a specific link
fragility23=rio::import(file = linkGit) ## code to use the package rio, import the file above and name it 'fragility23'
str(fragility23) ## code requests the data structure of 'fragility23' to see the types of data in each column
sqrt(fragility23[,4:5]) ## code requests the square root of all the values in columns 4 and 5 to show individual output values
sqrt(fragility23$Total) ## code requests the square root of each value in the total column
sqrt(fragility23$Total[1]) ## code requests the square root of the first value in the total column
sum(fragility23[,4:5]) ## code requests the sum of all values in column 4 and 5
print(apply(fragility23[,4:5],2,sum)) ## code requests the sum of all values in each column 4 and 5 to show as separate totals
apply(fragility23[,4:5],2,sum) ## code requests the sum of all values in each column 4 and 5 to show as separate totals
apply(fragility23[,4:5],1,sum) ## code requests the sum of all values in each column 4 and 5 to show as separate totals
View(fragility23)
apply(fragility23[,1:5],1,sum) ## code requests the sum of all values in each column 4 and 5 to show as separate totals
linkGit="https://github.com/DACSS-Fundamentals/overview/raw/refs/heads/main/FSI-2023-DOWNLOAD.xlsx" ## code to request a file from a specific link
fragility23=rio::import(file = linkGit) ## code to use the package rio, import the file above and name it 'fragility23'
str(fragility23) ## code requests the data structure of 'fragility23' to see the types of data in each column
sqrt(fragility23[,4:5]) ## code requests the square root of all the values in columns 4 and 5 to show individual output values
sqrt(fragility23$Total) ## code requests the square root of each value in the total column
sqrt(fragility23$Total[1]) ## code requests the square root of the first value in the total column
sum(fragility23[,4:5]) ## code requests the sum of all values in column 4 and 5
apply(fragility23[,4:5],2,sum) ## code requests the sum of all values in each column 4 and 5 to show as separate totals
typeof(apply(fragility23[,4:5],2,sum)) ## code requests the data type of the sums of the values in columns 4 and 5. Since it says 'double' and not list, it is a vector
class(apply(fragility23[,4:5],2,sum)) ## added this one for myself to see the difference between 'typeof' and 'class' for apply to compare to sapply and lapply
print(apply(fragility23[,4:5],1,sum)) ## code requests the sum of the values in each row
print(lapply(fragility23[,4:5],sum)) ## code requests the sum for each of the columns 4 and 5 to get a list instead of vectors; can only compute by column
typeof(lapply(fragility23[,4:5],sum)) ## code requests the data type of the sums for each of the columns, lapply gives us a list
class(lapply(fragility23[,4:5],sum)) ## code requests the data class of the sums for each of the columns, lapply gives us a list
print(sapply(fragility23[,4:5],sum)) ## code requests the sum of all values in each column 4 and 5 to show as separate totals; looks very similar to 'apply' function but gives
typeof(sapply(fragility23[,4:5],sum)) ## added this one for myself to see the difference between 'typeof' and 'class' for apply to compare to sapply and lapply
class(sapply(fragility23[,4:5],sum)) ## code requests the data class of the sums for each of the columns 4 and 5
print(lapply(fragility23[,4:5],sqrt)) ## code requests the square root of each values in columns 4 and 5
class(lapply(fragility23[,4:5],sqrt)) ## code requests the data class of the square root values and comes out with a list
typeof(lapply(fragility23[,4:5],sqrt)) ## added this one for myself to see the difference between 'typeof' and 'class' for apply to compare to sapply and lapply
print(sapply(fragility23[,4:5],sqrt)) ## code requests the square root of each of the values in columns 4 and 5
class(sapply(fragility23[,4:5],sqrt)) ## code requests the data class of the square root values and comes out with a matrix or array
typeof(sapply(fragility23[,4:5],sqrt)) ## added this one for myself to see the difference between 'typeof' and 'class' for apply to compare to sapply and lapply
linkGit="https://github.com/DACSS-Fundamentals/overview/raw/refs/heads/main/FSI-2023-DOWNLOAD.xlsx" ## code to request a file from a specific link
fragility23=rio::import(file = linkGit) ## code to use the package rio, import the file above and name it 'fragility23'
str(fragility23) ## code requests the data structure of 'fragility23' to see the types of data in each column
sqrt(fragility23[,4:5]) ## code requests the square root of all the values in columns 4 and 5 to show individual output values
sqrt(fragility23$Total) ## code requests the square root of each value in the total column
sqrt(fragility23$Total[1]) ## code requests the square root of the first value in the total column
sum(fragility23[,4:5]) ## code requests the sum of all values in column 4 and 5
apply(fragility23[,4:5],2,sum) ## code requests the sum of all values in each column 4 and 5 to show as separate totals
typeof(apply(fragility23[,4:5],2,sum)) ## code requests the data type of the sums of the values in columns 4 and 5. Since it says 'double' and not list, it is a vector
class(apply(fragility23[,4:5],2,sum)) ## added this one for myself to see the difference between 'typeof' and 'class' for apply to compare to sapply and lapply
print(apply(fragility23[,4:5],1,sum)) ## code requests the sum of the values in each row
print(lapply(fragility23[,4:5],sum)) ## code requests the sum for each of the columns 4 and 5 to get a list instead of vectors; can only compute by column
typeof(lapply(fragility23[,4:5],sum)) ## code requests the data type of the sums for each of the columns, lapply gives us a list
class(lapply(fragility23[,4:5],sum)) ## code requests the data class of the sums for each of the columns, lapply gives us a list
print(sapply(fragility23[,4:5],sum)) ## code requests the sum of all values in each column 4 and 5 to show as separate totals; looks very similar to 'apply' function but gives
typeof(sapply(fragility23[,4:5],sum)) ## added this one for myself to see the difference between 'typeof' and 'class' for apply to compare to sapply and lapply
class(sapply(fragility23[,4:5],sum)) ## code requests the data class of the sums for each of the columns 4 and 5
print(lapply(fragility23[,4:5],sqrt)) ## code requests the square root of each values in columns 4 and 5
class(lapply(fragility23[,4:5],sqrt)) ## code requests the data class of the square root values and comes out with a list
typeof(lapply(fragility23[,4:5],sqrt)) ## added this one for myself to see the difference between 'typeof' and 'class' for apply to compare to sapply and lapply
print(sapply(fragility23[,4:5],sqrt)) ## code requests the square root of each of the values in columns 4 and 5
class(sapply(fragility23[,4:5],sqrt)) ## code requests the data class of the square root values and comes out with a matrix or array
typeof(sapply(fragility23[,4:5],sqrt)) ## added this one for myself to see the difference between 'typeof' and 'class' for apply to compare to sapply and lapply
theOnesOK = function(DF_country_and_variable) { # input of the function
variable_values <- DF_country_and_variable[,2] #
avg_value <- mean(variable_values, na.rm = TRUE)
is_above <- ifelse(variable_values > avg_value, "Above Average", "Below/At Average")
DF_country_and_variable$Status <- is_above
return(DF_country_and_variable)
}
theOnesOK = function(DF_country_and_variable) { # input of the function
variable_values <- DF_country_and_variable[,2] #
avg_value <- mean(variable_values, na.rm = TRUE)
is_above <- ifelse(variable_values > avg_value, "Above Average", "Below/At Average")
DF_country_and_variable$Status <- is_above
return(DF_country_and_variable)
}
linkGit="https://github.com/DACSS-Fundamentals/overview/raw/refs/heads/main/FSI-2023-DOWNLOAD.xlsx" ## code to request a file from a specific link
fragility23=rio::import(file = linkGit) ## code to use the package rio, import the file above and name it 'fragility23'
str(fragility23) ## code requests the data structure of 'fragility23' to see the types of data in each column
sqrt(fragility23[,4:5]) ## code requests the square root of all the values in columns 4 and 5 to show individual output values
sqrt(fragility23$Total) ## code requests the square root of each value in the total column
sqrt(fragility23$Total[1]) ## code requests the square root of the first value in the total column
sum(fragility23[,4:5]) ## code requests the sum of all values in column 4 and 5
apply(fragility23[,4:5],2,sum) ## code requests the sum of all values in each column 4 and 5 to show as separate totals
typeof(apply(fragility23[,4:5],2,sum)) ## code requests the data type of the sums of the values in columns 4 and 5. Since it says 'double' and not list, it is a vector
class(apply(fragility23[,4:5],2,sum)) ## added this one for myself to see the difference between 'typeof' and 'class' for apply to compare to sapply and lapply
print(apply(fragility23[,4:5],1,sum)) ## code requests the sum of the values in each row
print(lapply(fragility23[,4:5],sum)) ## code requests the sum for each of the columns 4 and 5 to get a list instead of vectors; can only compute by column
typeof(lapply(fragility23[,4:5],sum)) ## code requests the data type of the sums for each of the columns, lapply gives us a list
class(lapply(fragility23[,4:5],sum)) ## code requests the data class of the sums for each of the columns, lapply gives us a list
print(sapply(fragility23[,4:5],sum)) ## code requests the sum of all values in each column 4 and 5 to show as separate totals; looks very similar to 'apply' function but gives
typeof(sapply(fragility23[,4:5],sum)) ## added this one for myself to see the difference between 'typeof' and 'class' for apply to compare to sapply and lapply
class(sapply(fragility23[,4:5],sum)) ## code requests the data class of the sums for each of the columns 4 and 5
print(lapply(fragility23[,4:5],sqrt)) ## code requests the square root of each values in columns 4 and 5
class(lapply(fragility23[,4:5],sqrt)) ## code requests the data class of the square root values and comes out with a list
typeof(lapply(fragility23[,4:5],sqrt)) ## added this one for myself to see the difference between 'typeof' and 'class' for apply to compare to sapply and lapply
print(sapply(fragility23[,4:5],sqrt)) ## code requests the square root of each of the values in columns 4 and 5
class(sapply(fragility23[,4:5],sqrt)) ## code requests the data class of the square root values and comes out with a matrix or array
typeof(sapply(fragility23[,4:5],sqrt)) ## added this one for myself to see the difference between 'typeof' and 'class' for apply to compare to sapply and lapply
theOnesOK(fragility23[,c('Country','S1: Demographic Pressures')]) ## code creates new file titled theOnesOK and includes only 2 columns: 'country' and 'S1 Demographic Pressures'
theOnesOK=(fragility23[,c('Country','S1: Demographic Pressures')]) ## code creates new file titled theOnesOK and includes only 2 columns: 'country' and 'S1 Demographic Pressures'
View(theOnesOK)
theOnesOK(fragility23[,c('Country','S1: Demographic Pressures')]) ## code creates new file titled theOnesOK and includes only 2 columns: 'country' and 'S1 Demographic Pressures'
theOnesOK = function(DF_country_and_variable) { # input of the function
variable_values <- DF_country_and_variable[,2] #
avg_value <- mean(variable_values, na.rm = TRUE)
is_above <- ifelse(variable_values > avg_value, "Above Average", "Below/At Average")
DF_country_and_variable$Status <- is_above
return(DF_country_and_variable)
}
theOnesOK(fragility23[,c('Country','S1: Demographic Pressures')]) ## code creates new file titled theOnesOK and includes only 2 columns: 'country' and 'S1 Demographic Pressures'
mystery=function(DF,positionsToUse,CountryColumn='Country'){
newDF=DF[,c(CountryColumn),drop = FALSE]
average='average'
newDF[,average]=apply(DF[,positionsToUse],1,mean,na.rm = TRUE)
return(newDF[,c(CountryColumn,average)])
}
mystery(fragility23[,c(1,4:6)]) ## code creates a new data frame called 'mystery' with columns 1, 4, 5, and 6
View(fragility23)
mystery(fragility23,4:6) ## code creates a new data frame called 'mystery' with columns 1, 4, 5, and 6
mystery(fragility23,4:6)
rmarkdown::convert_ipynb("Copy_of_Functions_in_R.ipynb")
rmarkdown::convert_ipynb("Copy_of_Functions_in_R.ipynb")
rmarkdown::convert_ipynb('Copy_of_Functions_in_R.ipynb')
rmarkdown::convert_ipynb("Copy_of_Functions_in_R.ipynb")
View(theOnesOK)
rmarkdown::convert_ipynb("Copy_of_Functions_in_R.ipynb")
rmarkdown::convert_ipynb("Copy_of_Functions_in_R")
rmarkdown::convert_ipynb("Copy_of_Functions_in_R.ipynb")
rmarkdown::convert_ipynb("Copy_of_Coercion_in_R.ipynb")
rmarkdown::convert_ipynb("Copy_of_Coercion_in_R.ipynb")
rmarkdown::convert_ipynb("Copy_of_Functions_in_R.ipynb")
rmarkdown::convert_ipynb("copy_of_functions_in_R.ipynb")
setwd("~/Desktop/GitHub Repos/Homework-2")
rmarkdown::convert_ipynb("copy_of_functions_in_R.ipynb")
rm(list = ls()) ## code to reset the environment
setwd("~/Desktop/GitHub Repos/Homework-2")
rm(list = ls()) ## code to reset the environment
rm(list = ls()) ## code to reset the environment
linkGit="https://github.com/DACSS-Fundamentals/overview/raw/refs/heads/main/FSI-2023-DOWNLOAD.xlsx" ## code to request a file from a specific link
fragility23=rio::import(file = linkGit) ## code to use the package rio, import the file above and name it 'fragility23'
countCharacter =  function(text){ # read a text
listOfCharacters=strsplit(text, split = "") # text into list characters
vectorOfCharacters=listOfCharacters[[1]] # list of characters as vector
#return(length(vectorOfCharacters))
length(vectorOfCharacters) # returning count of characters in text
}
View(countCharacter)
word='peru'
length(strsplit(word, split = "")[[1]])
countCharacter('peru')
# how readable is this?
'peru'|>strsplit(split = "")|>(\(x) x[[1]])()|>length()
nchar('peru and usa') # R had one already!
countCharacter('peru and usa')
# this is local to the function
# listOfCharacters
# install.packages("textcat")
countCharacter =  function(text){ # read a text
listOfCharacters=strsplit(text, split = "") # text into list characters
vectorOfCharacters=listOfCharacters[[1]] # list of characters as vector
#return(length(vectorOfCharacters))
length(vectorOfCharacters) # returning count of characters in text
}
word='peru'
length(strsplit(word, split = "")[[1]])
countCharacter('peru')
word='peru'
strsplit(word, split = ""
word='peru'
strsplit(word, split = "")
countCharacter('peru')[[1]]
word='peru'
strsplit(word, split = "")[[1]]
word='peru'
strsplit(word, split = "")
word='peru'
strsplit(word, split = "")[[1]]
word='peru'
length(strsplit(word, split = "")[[1]])
nchar('peru and usa') # R had one already!
countCharacter('peru and usa')
plot(cars)
listOfCharacters=word('peru')
listOfCharacters=('peru')
word='peru'
listOfCharacters=('peru')
word='peru'
listOfCharacters=(word)
word='peru'
length()=(word)
word='peru'
length(word)
word='peru'
listOfCharacters(word)
word='peru'
strsplit(word)
word='peru'
strsplit(word, split = "")
# install.packages("textcat")
# install.packages("textcat")
install.packages("textcat")
countCharacter =  function(text){
listOfCharacters=strsplit(text, split = "") #
vectorOfCharacters=listOfCharacters[[1]]
length(vectorOfCharacters)
}
View(countCharacter)
View(fragility23)
InfoText =  function(text){
textLanguage=textcat::textcat(text)
output=list(language=textLanguage,
counts=countCharacter(text))
return(output)
}
names(x=fragility23)
names[,c(1)]
raiseTheNum(4)
raiseTheNum=function(aNumber, thePower=2){ #notice default
aNumber^thePower
}
raiseTheNum(4)
raiseTheNum(4,0.5)
raiseTheNum(thePower = 0.5,4)
raiseTheNum(0.5,4)
giveMeStats= function(numerator,denominator){
theSum=numerator+denominator
theDiff=numerator-denominator
theProduct=numerator*denominator
theDivision=numerator/denominator
output=list(sum=theSum,
diff=theDiff,
product=theProduct,
division=theDivision)
return(output)}
giveMeStats(4,8) # order matches argument names!
giveMeStats(denominator = 4,8) # order does not matter if argument names are explicit!
rm(list = ls()) ## code to reset the environment
linkGit="https://github.com/DACSS-Fundamentals/overview/raw/refs/heads/main/FSI-2023-DOWNLOAD.xlsx" ## code to request a file from a specific link
fragility23=rio::import(file = linkGit) ## code to use the package rio, import the file above and name it 'fragility23'
str(fragility23) ## code requests the data structure of 'fragility23' to see the types of data in each column
str(fragility23) ## code requests the data structure of 'fragility23' to see the types of data in each column
sqrt(fragility23[,4:5]) ## code requests the square root of all the values in columns 4 and 5 to show individual output values
install.packages("rio")
str(fragility23) ## code requests the data structure of 'fragility23' to see the types of data in each column
View(fragility23)
sqrt(fragility23[,4:5]) ## code requests the square root of all the values in columns 4 and 5 to show individual output values
sqrt(fragility23$Total) ## code requests the square root of each value in the total column
sqrt(fragility23$Total[1]) ## code requests the square root of the first value in the total column
names(fragility23$Country[1])
names(fragility23$Country[2])
names(x=fragility23)
names(fragility23$Country[1])
names(fragility23[1])
names(fragility23[2])
rmarkdown::convert_ipynb("Marrone_Demo1.ipynb")
setwd("~/Desktop/GitHub Repos/Homework-1")
rmarkdown::convert_ipynb("Marrone_Demo1.ipynb")
View(fragility23)
theOnesOK = function(DF_country_and_variable) { # input of the function
variable_values <- DF_country_and_variable[,2] # variables includ
avg_value <- mean(variable_values, na.rm = TRUE)
is_above <- ifelse(variable_values > avg_value, "Above Average", "Below/At Average")
DF_country_and_variable$Status <- is_above
return(DF_country_and_variable)
}
View(theOnesOK)
theOnesOK(fragility23[,c('Country','S1: Demographic Pressures')])
theOnesOK = function(DF_country_and_variable) { # input of the function
variable_values <- DF_country_and_variable[,2] # variable value is a column of information
avg_value <- mean(variable_values, na.rm = TRUE) # find mean mean of the column and ignore missing values
is_above <- ifelse(variable_values > avg_value, "Above Average", "Below/At Average") # determine if each country's value is above average or below/at average
DF_country_and_variable$Status <- is_above # create a 'status' column to label each country with the status above
return(DF_country_and_variable) # return a data frame with 3 columns- country, S1 and status
}
theOnesOK(fragility23[,c('Country','S1: Demographic Pressures')])
theOnesOK(fragility23[,c('Country','S2: Refugees and IDPs')])
theOnesOK(fragility23[,c('Country','S1: Demographic Pressures')])
View(fragility23)
mystery=function(DF,positionsToUse,CountryColumn='Country'){ # input of the function
newDF=DF[,c(CountryColumn),drop = FALSE] # create a new data frame with the 'country' column named
average='average'
newDF[,average]=apply(DF[,positionsToUse],1,mean,na.rm = TRUE)
return(newDF[,c(CountryColumn,average)])
}
View(mystery)
mystery(fragility23,4:6)
mystery(fragility23,4:6)
theOnesOK(fragility23[,c('Country','S1: Demographic Pressures')])
typeof(theOnesOK(fragility23[,c('Country','S1: Demographic Pressures')])) # code
theOnesOK(fragility23[,c('Country','S1: Demographic Pressures')]) # code creates
mystery(fragility23,4:6) # code creates a new data frame with the country as a column and another column that holds the mean of columns 4, 5 and 6.
setwd("~/Desktop/GitHub Repos/Homework-2")
theOnesOK2 = function(DF, DFvariable, CountryColumn='Country') { # input of the function
variable_values <- DF[,DFvariable]
avg_value <- mean(variable_values, na.rm = TRUE)
is_above <- ifelse(variable_values > avg_value, "Above Average", "Below/At Average")
newname = paste('Status_on', DFvariable)
DF[,newname] <- is_above
return(DF[,c(CountryColumn, newname)])
}
theOnesOK2(fragility23[,c('Country','S1: Demographic Pressures')])
theOnesOK2 = function(DF, DFvariable, CountryColumn='Country') { # input of the function
variable_values <- DF[,DFvariable]
avg_value <- mean(variable_values, na.rm = TRUE)
is_above <- ifelse(variable_values > avg_value, "Above Average", "Below/At Average")
newname = paste('Status_on', DFvariable)
DF[,newname] <- is_above
return(DF[,c(CountryColumn, newname)])
}
theOnesOK2(fragility23[,c('Country','S1: Demographic Pressures')])
View(theOnesOK2)
theOnesOK2(fragility23[,c('country')]
View(fragility23)
theOnesOK2(fragility23)
theOnesOK2(fragility23[,'S1: Demographic Pressures'])
theOnesOK2(fragility23[,2])
theOnesOK2(fragility23[,1])
theOnesOK2(fragility23[,1])
theOnesOK2(fragility23, 4, CountryColumn = 'country')
theOnesOK2(fragility23[,c(4, CountryColumn = 'country')]
theOnesOK2(fragility23[,c(, CountryColumn = 'country')]
theOnesOK2(fragility23[,c(CountryColumn = 'country')])
View(fragility23)
theOnesOK2(fragility23[,c('S1: Demographic Pressure', CountryColumn = 'country')])
theOnesOK2(fragility23[,c])
theOnesOK2(fragility23)
theOnesOK2(fragility23[,c("Country")])
theOnesOK2(fragility23[,c("Country", "S1: Demographic Pressures")])
theOnesOK2(fragility23[,c("Country", "S1: Demographic Pressures", "newname")])
theOnesOK2(fragility23[,c("Country", "S1: Demographic Pressures")])
theOnesOK2(fragility23[,c("S1: Demographic Pressures")])
theOnesOK2(fragility23, 4:5)
theOnesOK2(fragility23, 4)
theOnesOK2(fragility23, "S1: Demographic Pressures")
theOnesOK2(fragility23, "S1: Demographic Pressures")
theOnesOK2(fragility23, "Country", "S1: Demographic Pressures")
theOnesOK2(fragility23, "S1: Demographic Pressures")
